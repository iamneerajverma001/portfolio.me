CityPulse – Traffic Intelligence System | Script Guide

Objective:
Detect vehicles, identify emergency classes, and update traffic signal timing.

1) Core pipeline
- Load YOLO model
- Read live stream/video frames
- Detect + classify vehicles
- Count lane-wise traffic density
- Detect ambulance/fire brigade class
- Send signal update to ESP32

2) Starter script
import cv2
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
cap = cv2.VideoCapture("traffic.mp4")

while cap.isOpened():
    ok, frame = cap.read()
    if not ok:
        break

    results = model(frame)

    # TODO: extract class ids and lane counts
    # TODO: emergency priority rule
    # TODO: send serial command to ESP32

cap.release()

3) Reuse notes
- Keep class mapping in a separate config
- Keep emergency-priority logic in one function
- Use one communication wrapper for ESP32 serial commands
